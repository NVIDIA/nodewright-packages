# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# ShellCheck linting for the repo's bash scripts.
#
# Errors are required today. Warnings and notes remain visible in annotations and
# the job summary while the existing debt is burned down. Rule configuration lives
# in .shellcheckrc.

name: ShellCheck

on:
  pull_request:
    branches: [main]
    paths:
      - '**/*.sh'
      - '.shellcheckrc'
      - '.github/workflows/lint-shellcheck.yaml'
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  shellcheck:
    name: shellcheck (errors required)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Show shellcheck version
        run: shellcheck --version

      # Register a problem matcher so findings surface as inline PR annotations.
      - name: Register shellcheck problem matcher
        run: |
          cat > "${RUNNER_TEMP}/shellcheck-matcher.json" <<'EOF'
          {
            "problemMatcher": [
              {
                "owner": "shellcheck",
                "pattern": [
                  {
                    "regexp": "^(.+):(\\d+):(\\d+):\\s+(warning|error):\\s+(.+)$",
                    "file": 1,
                    "line": 2,
                    "column": 3,
                    "severity": 4,
                    "message": 5
                  }
                ]
              }
            ]
          }
          EOF
          echo "::add-matcher::${RUNNER_TEMP}/shellcheck-matcher.json"

      # ubuntu-latest ships shellcheck preinstalled. Lint every tracked *.sh
      # file (excluding the Python venv) and keep the complete output for annotations.
      - name: Run shellcheck
        id: shellcheck
        run: |
          mapfile -t FILES < <(git ls-files '*.sh' | grep -v '^venv/')
          echo "Linting ${#FILES[@]} shell scripts"
          if [ "${#FILES[@]}" -eq 0 ]; then
            echo "No shell scripts found; nothing to lint."
            : > "${RUNNER_TEMP}/shellcheck.out"
            exit 0
          fi
          set +e
          shellcheck -f gcc "${FILES[@]}" | tee "${RUNNER_TEMP}/shellcheck.out"
          lint_status=${PIPESTATUS[0]}
          set -e
          echo "Full ShellCheck exit status (including advisory findings): ${lint_status}"

          # Keep all findings as annotations, but make only errors blocking until
          # the existing warning and note debt has been reduced.
          shellcheck --severity=error -f gcc "${FILES[@]}" | tee "${RUNNER_TEMP}/shellcheck.errors"

      - name: Summarize findings
        if: always()
        run: |
          OUT="${RUNNER_TEMP}/shellcheck.out"
          {
            echo "## ShellCheck (errors required)"
            echo ""
            if [ ! -s "$OUT" ]; then
              echo "No findings. :tada:"
              exit 0
            fi
            total=$(wc -l < "$OUT")
            errors=$(grep -c ': error:' "$OUT" || true)
            warnings=$(grep -c ': warning:' "$OUT" || true)
            notes=$(grep -c ': note:' "$OUT" || true)
            echo "| severity | count |"
            echo "|----------|-------|"
            echo "| error | ${errors} |"
            echo "| warning | ${warnings} |"
            echo "| note/style | ${notes} |"
            echo "| total | ${total} |"
            echo ""
            echo "Errors block this PR. Warnings and notes remain advisory while the existing debt is reduced. See the annotations and step log for details."
          } >> "$GITHUB_STEP_SUMMARY"
