[main]
include=nvidia-vr200-noreboot-base
summary=Optimized for inference workloads (BCM, no-reboot)

[sysctl]
# Minimize latency
vm.swappiness=1
# kernel.sched_latency_ns and kernel.sched_min_granularity_ns omitted: these CFS sysctls
# were removed in kernel 6.8+ (EEVDF replaced CFS) and do not exist on Ubuntu 26.04.
